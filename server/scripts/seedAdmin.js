require('dotenv').config(); const mongoose=require('mongoose'); const User=require('../src/models/User');
(async()=>{ await mongoose.connect(process.env.MONGO_URI); const email=process.env.ADMIN_EMAIL||'admin@freshfeast.local'; const password=process.env.ADMIN_PASSWORD||'admin123'; let user=await User.findOne({email}); if(!user) user=await User.create({name:'Fresh Feast Admin',email,password,role:'admin'}); else if(user.role!=='admin'){user.role='admin';await user.save();} console.log(`Admin ready: ${email}`); await mongoose.disconnect(); })().catch(e=>{console.error(e.message);process.exit(1)});
